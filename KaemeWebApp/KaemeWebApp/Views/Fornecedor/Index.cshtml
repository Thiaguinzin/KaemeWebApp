@model IEnumerable<Fornecedor>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary"> Fornecedores </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Fornecedor" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-person-plus"></i> &nbsp;Novo Fornecedor
            </a>
        </div>
    </div>
    <br>


    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Razão Social</th>
                <th scope="col">Endereço</th>
                <th scope="col">Contato</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var obj in Model)
            {
                <tr id="row_@obj.Id" class="table-success" style="vertical-align: middle;" onclick="location.href = '@(Url.Action("View", "Fornecedor", new { id = obj.Id }))'">
                    <td>
                        @obj.RazaoSocial
                    </td>
                    <td>
                        @obj.Endereco
                    </td>
                    <td>
                        @obj.Contato
                    </td>

                </tr>
            }
        </tbody>
    </table>

</div>